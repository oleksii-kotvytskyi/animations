import { Outlet } from "react-router-dom";

export const Day = () => {
  return (
    <div className="flex items-center justify-center h-screen w-full">
      Day ...
      <Outlet />
    </div>
  );
};
